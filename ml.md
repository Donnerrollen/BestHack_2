## 🧠 ML-часть: Векторизация и Поиск по CSV

ML-система проекта обеспечивает **семантический поиск адресов**, делая его устойчивым к ошибкам, сокращениям и различным вариантам написания, используя при этом **CSV-файл** для данных.

---

### 🚀 Используемая Модель

Мы используем специализированную версию модели **`cointegrated/rubert-tiny2`** .

* **База:** `rubert-tiny2` — компактная BERT-модель, хорошо работающая с русским языком.
* **Специализация (Fine-tuning):** Модель была **дообучена** (fine-tuned) на **созданном нами аугментированном датасете адресов**. Аугментация включала генерацию множества вариаций, опечаток и сокращений. Это позволяет модели научиться создавать **векторы (эмбеддинги)**, которые максимально близки для семантически одинаковых адресов, независимо от их текстового представления.

---

### ⚙️ Процесс Работы

#### 1. Векторизация и Хранение Данных

* **Данные:** Адреса загружаются из обычного **CSV-файла**.
* **Индексация:** Все адреса из CSV-файла пропускаются через дообученную модель для получения их **векторных представлений**.
* **Хранение:** Эти векторы сохраняются в **оперативной памяти сервера** и готового к быстрому поиску.

#### 2. Поиск по Косинусному Расстоянию

* **Запрос:** Пользовательский поисковый запрос векторизуется той же специализированной моделью.
* **Сравнение:** Полученный вектор запроса сравнивается со **всеми векторами** адресов, хранящимися в памяти.
* **Метрика:** Для определения сходства используется **косинусное расстояние** (или косинусное сходство), которое измеряет угол между векторами.
* **Результат:** Выбираются **топ-5 адресов** из исходного CSV-файла, чьи векторы имеют наибольшее сходство (наименьшее расстояние) с вектором запроса, предоставляя релевантные результаты семантического поиска.

---