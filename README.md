# BestHack_2

![Python](https://img.shields.io/badge/Python-3.13+-blue?logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-+-red?logo=fastapi)
![Docker](https://img.shields.io/badge/Docker-✓-blue?logo=docker)
![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-+-red?logo=sqlalchemy)
![Chroma](https://img.shields.io/badge/Chroma-+-green?logo=vector-database)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-+-blue?logo=postgresql)

## 🚀 Быстрый старт

### Предварительные требования

**Установите необходимые инструменты:**
- [Poetry](https://python-poetry.org/) - менеджер зависимостей Python
- [Docker](https://docs.docker.com/get-docker/) - контейнеризация приложения
- [Git](https://git-scm.com/) - система контроля версий

#### Установка на Ubuntu/Debian:
```bash
sudo apt update && sudo apt install -y git docker.io
curl -sSL https://install.python-poetry.org | python3 -
```

## 📦 Развертывание на сервере

### 1. Клонирование репозитория
```bash
git clone https://github.com/Donnerrollen/BestHack_2.git
cd BestHack_2
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе шаблона:
```bash
cp example.env .env
```

Отредактируйте файл с переменными окружения:
```bash
nano .env
```

> **Важно**: Самые актуальные переменные окружения находятся в файле `example.env`. Необходимо указать ВСЕ обязательные переменные.

### 3. Запуск сервера
```bash
make up
```

## 🛠 Makefile Команды

Проект использует Makefile для автоматизации различных задач разработки и развертывания:

### 🐳 Docker команды
```bash
make build     # Сборка Docker-образов
make up        # Запуск в режиме разработки
make prod      # Запуск в production-режиме
make test      # Запуск тестов
```

### 🔄 Миграции базы данных
```bash
make revision           # Создание новой миграции
make upgrade_head       # Применение всех миграций до последней версии
make migrate            # Создание и применение миграций
make downgrade          # Откат к предыдущей версии миграции
```

### 🎯 Разработка и код-стайл
```bash
make lint       # Проверка кода линтером (Ruff)
make format     # Форматирование кода
make fix        # Автоматическое исправление проблем кода
make typecheck  # Проверка типов (Pyright)
```

## 🔧 Настройка для разработки

### Установка инструментов разработки
```bash
# Установка Poetry
curl -sSL https://install.python-poetry.org | python3 -

# Альтернативная установка через pip
pip install poetry

# Установка системных зависимостей
sudo apt install -y docker.io git
```

## 📚 Установка зависимостей

### Production-режим
```bash
poetry install --no-dev
```

### Режим разработки
```bash
poetry install
```

## 🗄️ Технологии базы данных

Проект использует несколько технологий для работы с данными:

- **SQLAlchemy** - ORM для работы с реляционными базами данных
- **Chroma** - векторная база данных для работы с эмбеддингами
- **PostgreSQL** - основная реляционная база данных

## 🛠 Разработка

### Команды для разработки
```bash
make lint      # Проверка кода линтером
make fix       # Исправление кода (удаление неиспользуемых импортов и т.д.)
make format    # Форматирование кода
```

### Работа с миграциями
Для управления схемой базы данных используется Alembic:
```bash
make revision           # Создать новую миграцию
make upgrade_head       # Применить все pending миграции
make downgrade          # Откатить последнюю миграцию
```
